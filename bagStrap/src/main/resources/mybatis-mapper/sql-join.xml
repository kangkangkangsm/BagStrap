<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bagStrap.mapper.JoinMapper">
	<select id="joinList" parameterType="hashmap" resultType="com.example.bagStrap.model.User">
		SELECT * FROM TBL_USER
	</select>
	
	<insert id="joinInsert" parameterType="hashmap">
		INSERT INTO TBL_USER 
		(USER_NO,USER_ID, USER_NAME, USER_NICKNAME, PASSWORD, EMAIL, PHONE, BIRTH, GENDER) 
		VALUES (user_no_seq_NEXTVAL,#{userId}, #{userName}, #{userNickName}, #{password}, #{email}, #{phone}, #{birth}, #{gender})
	</insert>
	
	<!--아이디 중복검사-->
	<select id="joinMultiCheck" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_ID = #{userId}
	</select>
	
	<!--닉네임 중복검사-->
	<select id="nickNameMultiCheck" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM TBL_USER
		WHERE USER_NICKNAME = #{userNickName}
	</select>
	
	<!--주소추가-->
	<insert id="addressInsert" parameterType="hashmap">
		INSERT INTO TBL_ADDRESS
		( USER_ID, USER_NAME, PHONE, ADDRESS, ADDRESS_DETAIL, ZONECODE)
		VALUES ( #{userId}, #{userName}, #{phone}, #{address}, #{addressDetail}, #{zonecode})
	</insert>

	<select id="myInfoList" parameterType="hashmap" resultType="com.example.bagStrap.model.User">
		SELECT * FROM TBL_USER WHERE USER_ID=#{userId}
	</select>
	
	<select id="myInfoAddress" parameterType="hashmap" resultType="com.example.bagStrap.model.User">
		SELECT * 
		FROM TBL_ADDRESS 
		WHERE USER_ID=#{userId} 
	</select>

</mapper>